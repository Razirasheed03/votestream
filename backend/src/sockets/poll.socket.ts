// import { Socket } from "socket.io";

// export const registerPollSocket = (
//   socket: Socket
// ) => {
//   socket.on(
//     "poll:join",
//     ({ pollId }: { pollId: string }) => {
//       socket.join(`poll:${pollId}`);
//     }
//   );
// };
